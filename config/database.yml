default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV["CHAT_DATABASE_URL"] %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
  prepared_statements: false
